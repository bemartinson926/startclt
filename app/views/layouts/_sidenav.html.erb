<% if controller_name == 'users' || current_page?(new_group_path) ||
                                    current_page?(popular_groups_path) %>

	<%= render 'layouts/user_side_bar' %>

<% elsif controller_name == 'groups' %>

	<%= render 'layouts/group_side_bar' %>

<% end %>