

<%= bootstrap_form_for(@group, layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-7") do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.text_area :description %>
  </div>
  <div class="field">
      <%= hidden_field_tag(:user_id, current_user.id) %>
  </div>
  <div class="actions">
    <%= f.submit 'Create Group', class:'btn btn-lg btn-success'%>
  </div>
<% end %>
</div>
